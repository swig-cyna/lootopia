# @lootopia/common

Shared constants and validation schemas used by both the API and the frontend packages. Centralising these values prevents duplication and keeps validation logic consistent across the monorepo.

See [docs/common/](../../docs/common/README.md) for the list of exported constants and schemas.
