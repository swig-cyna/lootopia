# @lootopia/api

REST API for the Lootopia platform. Handles authentication, hunt management, player interactions, and exposes a fully typed OpenAPI spec consumed by the frontend clients.

See [docs/api/](../../docs/api/README.md) for architecture details, route conventions, services, middlewares, and pagination.
