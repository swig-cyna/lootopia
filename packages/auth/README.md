# @lootopia/auth

Authentication layer for the Lootopia platform. Exports both a server instance and a browser client, along with role constants and permission helpers used across the monorepo.

See [docs/auth/](../../docs/auth/README.md) for server setup, client usage, and role configuration.
