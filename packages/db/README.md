# @lootopia/db

Database layer for the Lootopia platform. Contains the schema definition, all migrations, typed repositories, and seed scripts. Every other package that needs database access imports repositories from here.

See [docs/db/](../../docs/db/README.md) for the schema diagram, migration workflow, and repository conventions.
